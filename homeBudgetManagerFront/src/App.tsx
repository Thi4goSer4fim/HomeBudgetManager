import AppRoutes from "./router/AppRouter";
/**
 * Componente raiz da aplicação.
 * Responsável por renderizar as rotas principais.
 */
const App: React.FC = () => {
  return <AppRoutes />;
};

export default App;
